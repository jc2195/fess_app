import 'package:flutter/material.dart';

class DownScreen extends StatelessWidget{
  @override

  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Oops, something went wrong here...'),
      ),
    );
  }
}